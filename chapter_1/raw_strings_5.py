# When you use "r" in print function then text treat as raw text without using escape sequence
print('Line A \n Line B') # \n work as a escape sequence
print(r"Line A \n Line B") # when use r before double quotes treat as raw text